// prisma/schema.prisma
generator client {
  provider = "prisma-client-js"   // ← yeh sahi hai

}

datasource db {
  provider = "postgresql"
}

model User {
  id Int @id @default(autoincrement())
  name String
  email String
  password String
  city String
  address String
  contact String
}

model Restaurant {
  id Int @id @default(autoincrement())
  email String? @unique
  password String?
  name String?
  address String?
  city String?
  contact String?
  foods     Foods[]
}

model Foods {
  id Int @id @default(autoincrement())
  name String
  price Int
  img_path String
  description String
  restaurant_id Int
  restaurant    Restaurant   @relation(fields: [restaurant_id], references: [id])
}


